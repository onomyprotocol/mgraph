{
  "name": "marketgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node undefined onomyprotocol/marketgraph",
    "create-local": "graph create --node http://localhost:8020/ onomyprotocol/marketgraph",
    "remove-local": "graph remove --node http://localhost:8020/ onomyprotocol/marketgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 onomyprotocol/marketgraph",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.58.0",
    "@graphprotocol/graph-ts": "0.30.0"
  }
}
